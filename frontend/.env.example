# Copy to .env.local for development. All values have working defaults.

# Flask API the Next route handlers talk to (server-side only — never
# exposed to the browser). In dev, Flask's dev server binds 127.0.0.1.
# In production, point this at the internal Flask service URL.
API_BASE_URL=http://127.0.0.1:5000
